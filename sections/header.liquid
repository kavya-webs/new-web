<style>
  /* Main Navbar Base & Sticky State */
  .site-header-wrapper {
    position: relative;
    width: 100%;
    z-index: 1000;
    background: #fff;
    box-shadow: 0 1px 8px rgba(0,0,0,0.06);
    transition: background 0.2s, box-shadow 0.2s;
  }
  .site-header-wrapper.is-sticky {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 1000;
    box-shadow: 0 4px 20px rgba(0,0,0,0.08);
  }

  /* Top Utility Announcement Bar */
  .announcement-bar {
    background-color: #ffffff;
    color: var(--color-text-heading);
    border-bottom: 1px solid rgba(0,0,0,0.06);
    padding: 8px 16px;
    font-size: 0.9rem;
    font-family: var(--font-body);
  }
  @media (min-width: 769px) {
    .announcement-bar {
      padding: 10px 80px;
    }
  }

  .announcement-bar__container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    margin: 0 auto;
    gap: 12px;
  }

  /* Left: Social Media Icons */
  .announcement-bar__socials {
    display: flex;
    align-items: center;
    gap: 14px;
  }
  .announcement-bar__social-link {
    color: var(--color-bg-dark);
    transition: transform 0.2s, color 0.2s;
    display: flex;
    align-items: center;
  }
  .announcement-bar__social-link:hover {
    color: var(--color-cta);
    transform: translateY(-1px);
  }

  /* Center: Sale Text & Live Countdown Timer */
  .announcement-bar__sale {
    display: flex;
    align-items: center;
    gap: 12px;
    margin: 0 auto;
  }
  .announcement-bar__sale-text {
    color: var(--color-cta);
    font-weight: 700;
    font-size: 0.95rem;
    letter-spacing: 0.5px;
  }
  .announcement-bar__timer {
    display: flex;
    align-items: center;
    gap: 6px;
    font-weight: 700;
  }
  .timer-pill {
    background: var(--color-bg-alternate);
    color: var(--color-cta);
    padding: 4px 10px;
    border-radius: 6px;
    display: inline-flex;
    align-items: baseline;
    gap: 3px;
    font-size: 0.95rem;
    font-weight: 700;
    border: 1px solid rgba(0, 149, 255, 0.15);
  }
  .timer-pill__unit {
    font-size: 0.6rem;
    font-weight: 700;
    text-transform: uppercase;
    color: var(--color-cta);
  }
  .timer-colon {
    color: var(--color-cta);
    font-weight: 700;
    font-size: 1rem;
  }

  /* Right: Currency & Language Selectors */
  .announcement-bar__selectors {
    display: flex;
    align-items: center;
    gap: 18px;
  }
  .announcement-bar__selector-item {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    color: var(--color-bg-dark);
    font-weight: 600;
    cursor: pointer;
    font-size: 0.9rem;
    transition: color 0.2s;
  }
  .announcement-bar__selector-item:hover {
    color: var(--color-cta);
  }
  .flag-icon {
    width: 20px;
    height: 14px;
    border-radius: 2px;
    object-fit: cover;
  }

  @media (max-width: 899px) {
    .announcement-bar {
      padding: max(4px, env(safe-area-inset-top)) 10px 4px 10px;
    }
    .announcement-bar__socials,
    .announcement-bar__selectors {
      display: none;
    }
    .announcement-bar__sale {
      flex-direction: column;
      justify-content: center;
      align-items: center;
      gap: 2px;
      width: 100%;
      text-align: center;
    }
    .announcement-bar__sale-text {
      font-size: 0.75rem;
      font-weight: 700;
      letter-spacing: 0.3px;
    }
    .announcement-bar__timer {
      gap: 3px;
    }
    .timer-pill {
      padding: 1px 5px;
      font-size: 0.72rem;
      border-radius: 4px;
    }
    .timer-pill__unit {
      font-size: 0.48rem;
    }

    .site-header {
      padding: 6px 12px;
      display: grid;
      grid-template-columns: auto 1fr auto;
      align-items: center;
    }
    .header-logo {
      font-size: 1.35rem;
      letter-spacing: 1px;
      justify-content: center;
    }
    .header-icons {
      gap: 8px;
    }
    .header-icon svg {
      width: 18px;
      height: 18px;
    }
  }

  /* Main Header Padding matching Hero Image */
  .site-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 14px 16px;
    background: #fff;
    width: 100%;
  }
  @media (min-width: 769px) {
    .site-header { padding: 16px 80px; }
  }
  @media (min-width: 900px) {
    .site-header {
      display: grid;
      grid-template-columns: 1fr auto 1fr;
    }
  }

  /* Logo (Left) */
  .header-logo {
    font-family: var(--font-heading);
    font-size: 2.2rem;
    font-weight: 600;
    color: var(--color-bg-dark);
    text-decoration: none;
    letter-spacing: 2px;
    display: flex;
    align-items: center;
  }

  /* Navigation (Center) */
  .header-nav { display: none; }
  @media (min-width: 900px) {
    .header-nav {
      display: flex;
      justify-content: center;
      gap: var(--spacing-md);
    }
  }
  .header-nav__link {
    font-family: var(--font-body);
    font-size: 0.95rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--color-text-heading);
    text-decoration: none;
    transition: color 0.3s;
  }
  .header-nav__link:hover {
    color: var(--color-cta);
    opacity: 1;
  }

  /* Icons / CTA (Right) */
  .header-icons {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    gap: 16px;
    margin-left: auto;
  }
  .header-icon {
    background: none;
    border: none;
    cursor: pointer;
    color: var(--color-text-heading);
    display: flex;
    align-items: center;
    transition: color 0.3s;
  }
  .header-icon:hover { color: var(--color-cta); }

  .header-quiz-btn { display: none; }
  @media (min-width: 900px) {
    .header-quiz-btn {
      display: inline-flex;
      margin-left: 16px;
      padding: 10px 24px;
      font-size: 0.75rem;
    }
  }

  /* Mobile Menu Icon */
  .header-mobile-menu {
    display: flex;
    align-items: center;
    margin-right: 16px;
  }
  @media (min-width: 900px) {
    .header-mobile-menu { display: none; }
  }

  /* Mobile Menu Drawer & Overlay */
  .mobile-menu-overlay {
    position: fixed;
    top: 0; left: 0;
    width: 100vw; height: 100vh;
    background: rgba(18,59,90,0.5);
    backdrop-filter: blur(2px);
    z-index: 9998;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
  }
  .mobile-menu-overlay.is-open {
    opacity: 1;
    pointer-events: auto;
  }

  .mobile-menu-drawer {
    position: fixed;
    top: 0; left: 0;
    width: 85%;
    max-width: 320px;
    height: 100vh;
    background: #fff;
    z-index: 9999;
    transform: translateX(-100%);
    transition: transform 0.35s cubic-bezier(0.16, 1, 0.3, 1);
    display: flex;
    flex-direction: column;
    box-shadow: 4px 0 25px rgba(0,0,0,0.15);
    padding: 24px 20px;
    overflow-y: auto;
  }
  .mobile-menu-drawer.is-open {
    transform: translateX(0);
  }

  .mobile-menu-drawer__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-bottom: 20px;
    border-bottom: 1px solid rgba(0,0,0,0.08);
    margin-bottom: 20px;
  }

  .mobile-menu-close {
    background: none;
    border: none;
    font-size: 2rem;
    line-height: 1;
    color: var(--color-text-heading);
    cursor: pointer;
    padding: 4px 8px;
  }

  .mobile-menu-drawer__nav {
    display: flex;
    flex-direction: column;
    gap: 16px;
    flex-grow: 1;
  }

  .mobile-menu-drawer__link {
    font-family: var(--font-body);
    font-size: 1.05rem;
    font-weight: 500;
    color: var(--color-text-heading);
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 1px;
    padding: 8px 0;
    border-bottom: 1px dashed rgba(0,0,0,0.05);
    transition: color 0.2s ease;
  }
  .mobile-menu-drawer__link:hover {
    color: var(--color-cta);
  }

  .mobile-menu-drawer__footer {
    margin-top: 30px;
    padding-top: 20px;
    border-top: 1px solid rgba(0,0,0,0.08);
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .mobile-menu-drawer__contact {
    font-size: 0.8rem;
    color: var(--color-text-body);
    line-height: 1.5;
  }
</style>

<!-- Top Utility Announcement Bar (Scrolls away naturally) -->
<div class="announcement-bar">
  <div class="announcement-bar__container">
    
    <!-- Left: Social Media Icons -->
    <div class="announcement-bar__socials">

      <a href="https://www.facebook.com/people/Seravolve/61590433214671/" class="announcement-bar__social-link" aria-label="Facebook" target="_blank">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
      </a>
      <a href="https://instagram.com/seravolve" class="announcement-bar__social-link" aria-label="Instagram" target="_blank">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/></svg>
      </a>
      <a href="https://pin.it/2rym1fiZH" class="announcement-bar__social-link" aria-label="Pinterest" target="_blank">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.373 0 0 5.372 0 12c0 5.084 3.163 9.426 7.627 11.174-.105-.949-.2-2.405.042-3.441.218-.937 1.407-5.965 1.407-5.965s-.359-.719-.359-1.782c0-1.668.967-2.914 2.171-2.914 1.023 0 1.518.769 1.518 1.69 0 1.029-.655 2.568-.994 3.995-.283 1.194.599 2.169 1.777 2.169 2.133 0 3.772-2.249 3.772-5.495 0-2.873-2.064-4.882-5.012-4.882-3.414 0-5.418 2.561-5.418 5.207 0 1.031.397 2.138.893 2.738.098.119.112.224.083.345l-.333 1.36c-.053.22-.174.267-.402.161-1.499-.698-2.436-2.889-2.436-4.649 0-3.785 2.75-7.262 7.929-7.262 4.163 0 7.398 2.967 7.398 6.931 0 4.136-2.607 7.464-6.227 7.464-1.216 0-2.359-.631-2.75-1.378l-.748 2.853c-.271 1.043-1.002 2.35-1.492 3.146C9.57 23.812 10.763 24 12 24c6.627 0 12-5.373 12-12 0-6.628-5.373-12-12-12z"/></svg>
      </a>
    </div>

    <!-- Center: Pre Navratri Sale Announcement Text -->
    <div class="announcement-bar__sale">
      <span class="announcement-bar__sale-text">✨ Pre Navratri Sale: Flat 10% OFF with Code <strong>FESTIVE10</strong> &mdash; Ends 1st Day of Navratri! Free Shipping Across India</span>
    </div>

    <!-- Right: Currency & Language Selectors -->
    <div class="announcement-bar__selectors">
      <div class="announcement-bar__selector-item">
        <svg width="18" height="13" viewBox="0 0 640 480" class="flag-icon">
          <rect width="640" height="160" fill="#f93"/>
          <rect y="160" width="640" height="160" fill="#fff"/>
          <rect y="320" width="640" height="160" fill="#128807"/>
          <circle cx="320" cy="240" r="60" fill="none" stroke="#008" stroke-width="6"/>
        </svg>
        <span>India(INR ₹)</span>
        <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="6 9 12 15 18 9"></polyline></svg>
      </div>

      <div class="announcement-bar__selector-item">
        <span>English</span>
        <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="6 9 12 15 18 9"></polyline></svg>
      </div>
    </div>

  </div>
</div>

<!-- Sticky Main Navbar Wrapper -->
<div class="site-header-wrapper">

  <!-- Main Header -->
  <header class="site-header">
    <!-- Left: Logo & Mobile Hamburger -->
    <div style="display: flex; align-items: center;">
      <button class="header-icon header-mobile-menu" id="MobileMenuTrigger" aria-label="Open navigation menu">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="3" y1="12" x2="21" y2="12"></line><line x1="3" y1="6" x2="21" y2="6"></line><line x1="3" y1="18" x2="21" y2="18"></line></svg>
      </button>
      <a href="{{ routes.root_url }}" class="header-logo">
        {{ shop.name }}
      </a>
    </div>

    <!-- Center: Navigation -->
    <nav class="header-nav">
      <a href="{{ routes.root_url }}" class="header-nav__link">Home</a>
      <a href="{{ routes.all_products_collection_url }}" class="header-nav__link">Shop</a>
      <a href="/pages/routines" class="header-nav__link">Routines</a>
      <a href="/pages/about-us" class="header-nav__link">About</a>
      <a href="/blogs/news" class="header-nav__link">Blog</a>
      <a href="/pages/contact" class="header-nav__link">Contact</a>
    </nav>

    <!-- Right: Icons & CTA -->
    <div class="header-icons">
      <a href="{{ routes.search_url }}" class="header-icon" aria-label="Search">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>
      </a>
      <a href="{{ routes.cart_url }}" class="header-icon" aria-label="Cart" id="HeaderCartIcon" style="position:relative;">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path><line x1="3" y1="6" x2="21" y2="6"></line><path d="M16 10a4 4 0 0 1-8 0"></path></svg>
        <span class="header-cart-badge" id="HeaderCartCountBadge" style="position:absolute;top:-6px;right:-8px;background:#0095FF;color:#FFF;font-size:0.65rem;font-weight:700;padding:1px 6px;border-radius:50px;min-width:18px;text-align:center;">{{ cart.item_count }}</span>
      </a>
      <a href="/pages/quiz" class="btn header-quiz-btn">Build Routine</a>
    </div>
  </header>
</div>

<!-- Mobile Drawer Overlay & Drawer Container -->
<div class="mobile-menu-overlay" id="MobileMenuOverlay"></div>
<aside class="mobile-menu-drawer" id="MobileMenuDrawer" aria-label="Mobile Navigation">
  <div class="mobile-menu-drawer__header">
    <a href="{{ routes.root_url }}" class="header-logo" style="font-size: 1.5rem;">
      {{ shop.name }}
    </a>
    <button class="mobile-menu-close" id="MobileMenuClose" aria-label="Close navigation menu">&times;</button>
  </div>

  <nav class="mobile-menu-drawer__nav">
    <a href="{{ routes.root_url }}" class="mobile-menu-drawer__link">Home</a>
    <a href="{{ routes.all_products_collection_url }}" class="mobile-menu-drawer__link">Shop</a>
    <a href="/pages/routines" class="mobile-menu-drawer__link">Routines</a>
    <a href="/pages/about-us" class="mobile-menu-drawer__link">About Us</a>
    <a href="/blogs/news" class="mobile-menu-drawer__link">Blog</a>
    <a href="/pages/faq" class="mobile-menu-drawer__link">FAQ</a>
    <a href="/pages/contact" class="mobile-menu-drawer__link">Contact Us</a>
  </nav>

  <div class="mobile-menu-drawer__footer">
    <a href="/pages/quiz" class="btn" style="width: 100%; text-align: center;">Build Your Routine</a>
    <div class="mobile-menu-drawer__contact">
      <p><strong>Email:</strong> info@seravolve.com</p>
      <p><strong>Instagram:</strong> @seravolve</p>
    </div>
  </div>
</aside>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const trigger = document.getElementById('MobileMenuTrigger');
  const drawer = document.getElementById('MobileMenuDrawer');
  const overlay = document.getElementById('MobileMenuOverlay');
  const closeBtn = document.getElementById('MobileMenuClose');
  const drawerLinks = document.querySelectorAll('.mobile-menu-drawer__link');

  if (!trigger || !drawer || !overlay) return;

  const openDrawer = () => {
    drawer.classList.add('is-open');
    overlay.classList.add('is-open');
    document.body.style.overflow = 'hidden';
  };

  const closeDrawer = () => {
    drawer.classList.remove('is-open');
    overlay.classList.remove('is-open');
    document.body.style.overflow = '';
  };

  trigger.addEventListener('click', openDrawer);

  if (closeBtn) {
    closeBtn.addEventListener('click', closeDrawer);
  }

  overlay.addEventListener('click', closeDrawer);

  drawerLinks.forEach(link => {
    link.addEventListener('click', closeDrawer);
  });

  // Live Header Countdown Timer
  const targetDate = new Date('2026-08-28T23:59:59+05:30');

  function updateCountdown() {
    const now = new Date().getTime();
    const distance = targetDate.getTime() - now;

    if (distance < 0) return;

    const days = Math.floor(distance / (1000 * 60 * 60 * 24));
    const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((distance % (1000 * 60)) / 1000);

    const dEl = document.getElementById('timer-days');
    const hEl = document.getElementById('timer-hrs');
    const mEl = document.getElementById('timer-min');
    const sEl = document.getElementById('timer-sec');

    if (dEl) dEl.textContent = days < 10 ? '0' + days : days;
    if (hEl) hEl.textContent = hours < 10 ? '0' + hours : hours;
    if (mEl) mEl.textContent = minutes < 10 ? '0' + minutes : minutes;
    if (sEl) sEl.textContent = seconds < 10 ? '0' + seconds : seconds;
  }

  updateCountdown();
  setInterval(updateCountdown, 1000);

  // Sticky Navbar Scroll Handler
  const headerWrapper = document.querySelector('.site-header-wrapper');
  const announcementBar = document.querySelector('.announcement-bar');

  if (headerWrapper && announcementBar) {
    const handleScroll = () => {
      const threshold = announcementBar.offsetHeight || 40;
      if (window.scrollY > threshold) {
        if (!headerWrapper.classList.contains('is-sticky')) {
          headerWrapper.classList.add('is-sticky');
          document.body.style.paddingTop = `${headerWrapper.offsetHeight}px`;
        }
      } else {
        if (headerWrapper.classList.contains('is-sticky')) {
          headerWrapper.classList.remove('is-sticky');
          document.body.style.paddingTop = '0px';
        }
      }
    };

    window.addEventListener('scroll', handleScroll, { passive: true });
    handleScroll();
  }
});
</script>

{% schema %}
{
  "name": "Header",
  "settings": []
}
{% endschema %}
